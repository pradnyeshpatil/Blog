<% provide(:title, 'All posts') %>
<li><%= link_to "Export CSV", posts_path(format: :csv) %></li>
<h1>All post</h1>
<%= will_paginate %>

<ul class="posts">
<%= render @posts %>
</ul>
<%= will_paginate %>