<% provide(:title, 'All posts') %>
<%= link_to "Export CSV", posts_path(format: :csv), class: "btn btn-lg btn-primary" %>
<h1>All post</h1>
<%= will_paginate %>

<ul class="posts">
<%= render @posts %>
</ul>
<%= will_paginate %>